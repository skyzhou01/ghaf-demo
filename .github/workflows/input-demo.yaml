name: Inputs Demo Env
on: 
  workflow_dispatch:
    inputs:
      environment:
        description: Choose the environment to deploy
        required: true
        default: dev
jobs:
  inputs-demo-env:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the app
        run: echo "Deploying the application to ${{ inputs.environment }}"
